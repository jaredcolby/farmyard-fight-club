# Optional WebSocket overrides for local development.
# Leave these blank to rely on the Vite same-origin proxy.
# Leave host/port empty to use window.location via makeWsUrl so Vite's proxy stays in play.
VITE_WS_HOST=
VITE_WS_PORT=
VITE_WS_PATH=/ws
# VITE_WS_URL=
